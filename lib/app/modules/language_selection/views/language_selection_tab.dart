part of 'language_selection_view.dart';

class LanguageSelectionTab extends StatelessWidget {
  const LanguageSelectionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const LanguageSelectionMobile();
  }
}
